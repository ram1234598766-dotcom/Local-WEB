// LocalWEB Desktop Frontend Entry Point
import './styles.css';
import { initApp } from './app.js';

// Initialize the app when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  initApp();
});